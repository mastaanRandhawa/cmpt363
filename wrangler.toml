name = "cmpt363"
compatibility_date = "2025-04-01"

# Serve the Vite build output as static assets (works with both
# `wrangler deploy` and `wrangler pages deploy dist`)
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
